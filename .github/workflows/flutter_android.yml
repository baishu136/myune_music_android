name: Flutter Android Build

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - name: Update GitHub Release notes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release edit v0.9.9-android.325 \
            --notes "🚫此版本为修复无法导入歌曲bug的更新推送，含有较多bug并存在卡顿问题，若您已安装旧版，则不建议更新此版本，请稍作等待，1.0版本将很快发布"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.1'
          cache: true

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: flutter pub get

      - name: Run focused tests
        run: |
          flutter test test/project_changelog_test.dart
          flutter analyze --no-fatal-infos

      - name: Build arm64 release APK
        run: flutter build apk --release --target-platform android-arm64 --split-per-abi

      - name: Stage APK
        run: |
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
            dist/Myune-Music-0.9.9-android.325-arm64-v8a.apk
          sha256sum dist/*.apk > dist/SHA256SUMS.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Myune-Music-0.9.9-android.325
          path: dist/
          if-no-files-found: error
          retention-days: 30
