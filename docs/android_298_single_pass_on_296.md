# Android 0.9.9+298：在 296 完整基线上采用单份字形分段渲染

- 基线恢复为完整的 0.9.9+296，保留 266—296 的全部功能、动画、修复与性能优化。
- 逐字歌词的活动片段只绘制一份字形，通过 `srcATop` 在已绘制像素内推进高亮颜色，同时保留既有上浮曲线。
- 不再绘制未播放基线副本，也不使用差集裁剪或清除混合，避免重影、截断与歌词消失。
- 无效范围、无法获取字形边界或无法映射时间戳的文本继续通过静态排版显示，避免出现整行不可见。
- 缓存已完成、未开始和当前片段的排版结果，播放进度仅触发当前绘制器重绘，不重建歌词列表。
