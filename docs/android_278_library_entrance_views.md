# 0.9.9-android.278

## 根因

- 音乐库有普通列表和 A–Z 索引紧凑卡片两套独立渲染分支。
- 此前入场动画只包装了普通列表的歌曲条目，A–Z 索引卡片直接返回静态组件。
- 因此在 A–Z 查看方式下，即使动画控制器已经正确启动，页面上也没有任何组件监听它。

## 修复

- 抽取两种查看方式共用的歌曲入场过渡组件。
- 普通列表和 A–Z 卡片均使用 720 毫秒错峰动画，前五项依次淡入并从下方平滑移入。
- 保留音乐库载入完成、首帧后启动以及单次领取机制。
- 新增组件级测试，直接验证动画开始、中间和结束状态的透明度及纵向位移，不再只验证启动条件。
