# 0.9.9-android.325 歌曲导入修复

## 修复

- Android 端不再把包含冷门音频扩展名的自定义过滤器交给系统文件选择器，避免部分设备抛出 `Unsupported filter`。
- Android 端改为选择后由应用校验文件扩展名；桌面端继续使用系统扩展名过滤。
- 文件选择异常会写入错误日志并显示明确提示，导入处理会等待完成后再返回。
- 重复歌曲比较改为规范化路径并忽略大小写。

## 构建

- 版本：`0.9.9-android.325`
- 目标：Android arm64-v8a release APK
