# Android 301：连续渐变高亮

## 改动

- 保留 300 版本的 Unicode 字素级逐字上浮。
- 颜色高亮不再由每个字符各自从头推进，而是沿当前词的实际排版宽度形成一个连续前沿。
- 前沿经过字符内部时使用柔和羽化，已经经过的字符保持高亮，尚未到达的字符保持未播放颜色。
- 上浮进度和颜色进度相互独立：字符仍依次柔和跃起，视觉颜色则连续流过相邻字符。

## 性能

字符宽度仅从已有排版框计算，不增加整行测量；仍只有当前播放词使用逐字绘制，其余内容沿用合并缓存。
