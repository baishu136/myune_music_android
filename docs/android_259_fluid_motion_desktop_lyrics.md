# Android 259：流体持续运动与桌面歌词关闭稳定性

日期：2026-09-11

## 流体背景

- 省电、自动、流畅档位的运动周期由 13 / 11 / 9.25 秒再次减半为 6.5 / 5.5 / 4.625 秒，相对 258 版速度提升一倍。
- 各档刷新率仍保持 24 / 30 / 60 FPS；没有增加逐秒绘制帧数、Shader 色团或 uniform 数量。
- 流体运动不再由音频播放状态控制。歌曲暂停时背景继续流动；页面不可见、应用进入后台、减少动态效果或路由过渡时仍会停止调度，避免隐藏绘制。

## 桌面歌词

- 淡出完成后，被移除的原生悬浮 View 保持透明，不再立即恢复到完全不透明，避免 WindowManager 延迟提交移除时出现一帧回闪。
- 下一次显示时仅在重新挂载前设置为透明，并继续使用 180 毫秒淡入。
- 保留 160 毫秒关闭淡出、快速开关代际保护，以及锁定和淡出期间的点击穿透。

## 验证

- `flutter analyze --no-pub`：通过，0 个问题。
- `flutter test --no-pub`：通过，195 项测试全部成功。
- Android arm64 release 构建：通过，Kotlin 悬浮窗关闭路径与流体背景 Shader 均完成编译。
- APK：`com.myune.music`，`versionName 0.9.9`，`versionCode 2259`，包含 `lib/arm64-v8a/libapp.so`。
- APK v2 签名验证通过；SHA-256：`273DA2732319512D25A367963771CD16AA3FDDF705277F69DEBB4E7B9C98DC7A`。
- 构建时 ADB 未检测到设备，未执行自动安装及真机悬浮窗关闭帧验证。
