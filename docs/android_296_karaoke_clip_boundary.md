# Android 296：逐字歌词裁剪边界修复

## 问题

逐字高亮推进时，底层歌词在播放前沿被裁掉，但上浮高亮的透明渐变提前跨过该前沿，导致交界处同时缺少底层与完整高亮，视觉上表现为字形被竖向截断。

## 调整

- 高亮渐变从实际播放前沿开始，保证播放侧直到交界处均为完整不透明。
- 羽化范围仅延伸到尚未播放的一侧，继续保留柔和的逐字过渡。
- 同时覆盖从左到右与从右到左文本，并增加边界坐标测试，避免后续再次产生裁剪缺口。

## 性能

不新增绘制层、滤镜或逐帧对象，仅修正现有渐变端点计算，保持原有渲染开销。
