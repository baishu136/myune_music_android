# 0.9.9-android.249 歌词与播放页动画修正

- 默认歌词不再为每个目标启动 680 ms easeInOut 动画，改为临界阻尼的连续跟随。密集换行时保留当前速度，新目标可立即接管，避免重复慢起步。
- 歌词字体、模糊等非列表位移过渡恢复为 520 ms，不改变 248 版的屏外保留和翻译防裁剪修复。
- 播放页继续使用现有 520 ms Cupertino 滑入、封面 Hero 和页面快照，在最外层增加 72% 到 100% 的轻柔透明度变化。
- 连续跟随只在目标改变期间使用一个 ticker，完成后自动停止，不新增常驻动画或长期缓存。
