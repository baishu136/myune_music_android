# Android 288：逐字歌词换行位移

日期：2026-09-14

## 修改

- 逐帧对照参考视频与 Myune 现版本视频的逐字歌词换行阶段。
- 当前行切换后，上一行沿滚动方向额外轻移 2.8%，下一行从下方 5% 的位置柔和归位。
- 位移与 620 毫秒的列表滚动、聚焦缩放和明暗过渡使用同一缓动曲线，形成更清晰的离场、接续和聚焦层次。
- 当前播放行始终回到零位移，不改变歌词的视觉锚点或手动浏览行为。

## 性能

- 使用 `AnimatedSlide` 的合成层平移，不重新测量文字，也不改变列表项目高度。
- 不增加逐字歌词的 Widget 数量、滚动控制器或逐帧布局计算。
- 位移限制在行高 5% 内，并继续使用歌词视口的预留渲染范围，避免快速切行时提前卸载。

## 验证

- 覆盖离场行位移方向、幅度、时长和曲线。
- 覆盖缩放与位移叠加后的安全绘制范围。
- 执行静态分析、完整 Flutter 测试与 arm64 release 构建。
