# Android 0.9.9+279 首页载入动画稳定性

- 修复默认列表的载入动画与首批封面解码同时开始，真机首个可见帧可能已经错过动画的问题。
- 移除 A–Z 字母分组内逐卡片重复计算的错峰位移，避免多个分组同时启动时出现抽搐。
- 默认列表与 A–Z 查看方式统一使用内容区整体淡入和 42 像素上移动画。
- 动画在首帧完成后额外等待 72 毫秒启动，让首批列表布局与封面解码先稳定。
- 保持懒加载、封面预取和列表交互方式不变，不增加持续运行的动画或后台任务。
