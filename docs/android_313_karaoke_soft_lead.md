# 313：逐字歌词柔和首字母上浮

## 修改

- 字形最终上浮幅度由排版行高的 7.5% 调整为 9%，限制范围由 1.5—5 像素调整为 1.8—6 像素。
- 将位移进度从颜色高亮进度中分离；颜色继续沿用 311—312 的逐字推进与时间边界。
- 位移窗口由 `1.8 / (字素数 + 0.8)` 放宽为 `2.25 / (字素数 + 0.8)`，上限由 48% 放宽到 56%。
- 首字母在更宽的窗口内建立位移，降低单位时间内的上升速度；后续字形继续通过局部衰减波柔和接管。
- 保留 312 的六次幂范数跟随合成，避免重新引入硬最大值切换造成的速度折点。

## 性能边界

- 不新增 Widget、Ticker、离屏层、文本测量或缓存。
- 位移仍复用现有词元进度，每个活动字形只增加固定数量的浮点运算。

## 验证

- 逐字歌词与更新日志专项测试共 74 项通过；完整测试共 236 项通过。
- 对本次涉及的 4 个 Dart 文件执行静态分析，结果为 `No issues found`。
- APK 包信息：`versionCode=2313`、`versionName=0.9.9`，包内仅含 `arm64-v8a` 原生库。
- APK Signature Scheme v2 验证通过，16KB 页面对齐验证通过。
- APK SHA-256：`3D1360195823A254B6EAD169C461E92DEAB36FB830D81A6CE9F94AA9B963E064`。
- 已覆盖安装到 V2352A 真机，设备报告 `versionCode=2313`、`primaryCpuAbi=arm64-v8a`。
- 完成 18 秒真机录屏，并以 33ms 间隔复查首字母上浮：位移连续单调，未见字形消失、反向跳动或硬切换；运行日志无 Flutter/AndroidRuntime 异常。

## 产物

- APK：`F:\AGENT\1\myune_music_android\dist\Myune-Music-0.9.9-android.313-arm64-v8a.apk`
- 真机复查：`F:\AGENT\1\video_karaoke_313_review`
- 独立留档：`F:\临时文件夹\备份\Myune-Music-0.9.9+313-20260916-084048`
