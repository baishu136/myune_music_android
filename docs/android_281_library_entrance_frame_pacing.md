# Android 0.9.9+281 首页载入动画帧节奏

- 默认列表完整保留旧版首页动画；A–Z 网格只处理首批五个歌曲卡片，第六项及之后直接保持正常显示，避免创建大量并发透明合成层。
- 为参与动画的歌曲卡片增加独立重绘边界，动画期间复用已经绘制的卡片内容，仅更新透明度和位移。
- 动画播放期间通过性能协调器推迟封面暖缓存等非即时后台任务；首屏封面、列表布局及用户操作不被延迟。
- 保持原有 720 毫秒时长、五段错峰间隔、位移距离及缓动曲线不变。
