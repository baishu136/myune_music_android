# 316：逐字歌词跳转、短词、中文与翻译优化

## 修改

- 可见的非活动逐字歌词行提前保留单通道绘制面与字形布局，跳转时复用已塑形的字素数据。
- 缓存逐字歌词高度测量结果；活动行颜色变化只重建轻量绘制样式，不再重新计算每个字形的几何布局。
- 单字、双字短词可与相邻时间片组成连续接力链；相邻 token 间隔超过 220ms 时自动断开，避免跨停顿提前抬字。
- 中文逐字歌词按字素顺序跨相邻单字 token 传递上浮，后一汉字同样延迟启动，跟随阶段最多保留前一字 80% 的高度。
- 只允许第 1 行原文进入逐字着色和上浮范围；翻译行始终使用未播放颜色，并从高亮外发光中排除。
- “所有歌词”模式仅为原文合成逐字时间轴，翻译行不再生成虚拟高亮 token。

## 保留行为

- 9% 最终上浮高度、314 的 40% 降速、逐字颜色时间戳及 315 的连续接力方向保持不变。
- 手动浏览时的整行候选高亮、普通歌词模式及其他播放页功能不变。

## 验证

- 逐字歌词与版本记录专项测试：78 项通过。
- 完整 Flutter 测试：240 项通过。
- 本次涉及的 Dart 文件静态分析：无问题。
- 仅构建 `arm64-v8a` release APK；版本号 `0.9.9-android.316`，Android `versionCode=2316`。
- APK 通过 16 KB zipalign 与 v2 签名校验，SHA-256：`7B23D50C2AD4C11B03E4CFC0E46E2E690385FCA20985CBC3CB5EC9DB417CE841`。
- 已安装到 V2352A（arm64-v8a）并完成启动冒烟检查。

## 产物

- APK：`dist/Myune-Music-0.9.9-android.316-arm64-v8a.apk`
- 真机截图：`F:/AGENT/1/myune_316_nav.png`
- 独立留档：`F:/临时文件夹/备份/Myune-Music-0.9.9+316-20260916-133225`
