# Android 304：逐字高亮淡入

## 改动

- 每个字符不再在渐变前沿通过时直接达到高亮色。
- 渐变首次覆盖字符时只显示低强度高亮，前沿经过字符中心和尾部后再逐渐达到最终亮度。
- 淡入强度由渐变覆盖范围与完全通过范围共同计算，并使用两端零速度的 S 型曲线。
- 字符淡入、跨字符渐变和逐字上浮使用独立进度，三种效果可以自然叠加。

## 渲染安全

淡入在原有单份字形图层内通过透明度调制完成，不在基线或位移位置叠加另一份文字，因此不会重新引入重影、边缘增粗或差集裁剪造成的截断。
