# 310：逐字歌词连贯上浮波

## 修改

- 保留 309 的逐字渐变高亮、匀速视觉时钟、短词按时完成及字形缓存。
- 当前字符上浮时，允许后续字符提前跟随；跟随距离每增加一字，最大高度降低 15 个百分点。
- 后续高度序列为 85%、70%、55%、40%、25%；下一档 10% 低于 15% 阈值，不再触发。
- 跟随字符随距离增加延迟，且可在自身高亮开始后连续接管到完整高度，避免离散跳变。
- 上浮曲线改为两端速度和加速度都连续的 smootherstep，弱化僵硬的启动和停止。

## 性能约束

- 跟随仅复用已有字形进度做常数次浮点计算，不增加离屏图层、Ticker、文本排版或组件重建。
- 高亮颜色仍仅对实际播放中的相邻字符使用局部遮罩；提前跟随的灰字直接使用缓存绘制。

## 验证

- 新增跟随高度严格递减、15% 截止及完成态一致性的测试。
- 继续运行逐字亮度、文字可见性、快速词及时性、跳转、滚动和边界回归。
- 静态检查无问题；全套 234 项测试通过。歌词与更新记录专项测试新增跟随波全程连续、无向下跳变检查。
- arm64-v8a release APK 构建完成，versionCode 2310；APK Signature Scheme v2 与 16KB zipalign 检查通过。
- SHA256：`38D6D7883AEB9E16C7B57C691650DED355E15ACCE95D61EDE7A9C6B5BA4C9F9F`。
- 构建完成后连接的 V2352A 已从 ADB 设备列表断开，重启 ADB 服务后仍未恢复，因此本次无法执行 310 真机录屏；未以自动测试替代并声称真机验证。
